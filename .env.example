# Optional: set when you connect a custom domain on Vercel so canonical URLs,
# Open Graph links, sitemap, and JSON-LD use your production domain.
# In Vercel: Project → Settings → Environment Variables → add for Production (and Preview if needed).
# NEXT_PUBLIC_SITE_URL=https://www.example.com
